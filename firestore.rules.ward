type Product = {
  name: string,
  imagePath: string,
  latestPrice: int
}

match /products/{id} is Product {
  allow read: true;
  allow create, update: request.auth != null;
  allow delete: false;
}
