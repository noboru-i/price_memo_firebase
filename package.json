{
  "name": "price_memo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run fireward && firebase emulators:exec --only firestore 'mocha --exit --require ts-node/register test/*.spec.ts'",
    "start:emulator": "firebase emulators:start --only firestore",
    "fireward": "fireward -i firestore.rules.ward > firestore.rules",
    "predeploy": "npm run fireward",
    "deploy": "firebase deploy"
  },
  "author": "noboru-i",
  "license": "ISC",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.1",
    "@types/mocha": "^9.0.0",
    "firebase": "^9.6.2",
    "fireward": "^2.0.19",
    "mocha": "^9.1.3",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
